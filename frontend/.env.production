# Empty base => the app calls same-origin "/api", which Vercel routes to the
# Python function. Do not put a full URL here; frontend and API share a domain.
REACT_APP_BACKEND_URL=
# Don't treat CRA lint warnings as build-breaking errors on Vercel.
CI=false
